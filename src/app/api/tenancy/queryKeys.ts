import { MODULE_NAME } from './constants';

const queryKeys = {
  getNikeSKURedirectKey: () => [MODULE_NAME, 'nike-sku-redirect-key'],
  getNikeSKUUser: () => [MODULE_NAME, 'nike-sku-user'],
};

export default queryKeys;
